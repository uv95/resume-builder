import React from 'react';
import SettingsCard from '../SettingsCard';
import style from './Header.module.scss';

type Props = {};

const Header = (props: Props) => {
  return <SettingsCard title="Header">Header</SettingsCard>;
};

export default Header;
