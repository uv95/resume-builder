import Card from '@/components/Card/Card';
import React from 'react';
import style from './Colors.module.scss';

type Props = {};

const Colors = (props: Props) => {
  return (
    <Card>
      <h3 className="p-2">Colors</h3>
    </Card>
  );
};

export default Colors;
